const Header = () => {
  return (
    <header className="header">
      <h1>Redux Auth</h1>
        <nav>
          <ul>
            <li>
              <button>Logout</button>
            </li>
          </ul>
        </nav>
    </header>
  );
};

export default Header;
